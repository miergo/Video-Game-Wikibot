# Frontend

React + TypeScript + Vite chat UI for the ARC Raiders WikiBot.

See the root [README.md](../README.md) for full setup.

```bash
npm install
npm run dev    # http://localhost:5173 (proxies /api to the backend)
```
